version: "3.9"
services:
  api:
    build: .
    command: uvicorn ml_platform.serving.api:app --host 0.0.0.0 --port 8000
    environment:
      - MODEL_DIR=/app/artifacts/model
      - OUTPUT_DIR=/app/outputs
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
